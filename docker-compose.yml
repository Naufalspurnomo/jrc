services:
  jrc-db:
    image: "${JRC_DATABASE_IMAGE:-postgres@sha256:051f7b7b3abdd564d5d1bd1e8c4b9c1b6e77087d1dd22020ede611c096a272e0}"
    restart: unless-stopped
    environment:
      POSTGRES_DB: "${POSTGRES_DB:?POSTGRES_DB is required}"
      POSTGRES_USER: "${POSTGRES_USER:?POSTGRES_USER is required}"
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}"
    volumes:
      - jrc-database:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$${POSTGRES_USER}\" -d \"$${POSTGRES_DB}\""]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    security_opt:
      - no-new-privileges:true
    networks:
      - jrc-private

  jrc-api:
    image: "${JRC_API_IMAGE:?JRC_API_IMAGE is required}"
    build:
      context: ./server
      dockerfile: Dockerfile
      target: production
      args:
        RELEASE_SHA: "${RELEASE_SHA:?RELEASE_SHA is required}"
    restart: unless-stopped
    env_file:
      - path: ./server/.env
        required: false
    environment:
      NODE_ENV: production
      PORT: "3000"
      DATABASE_URL: "${COMPOSE_DATABASE_URL:?COMPOSE_DATABASE_URL is required}"
    depends_on:
      jrc-db:
        condition: service_healthy
        restart: true
      jrc-migrate:
        condition: service_completed_successfully
    ports:
      - "${API_BIND_ADDRESS:-127.0.0.1}:${API_PORT:-3010}:3000"
    volumes:
      - jrc-private-storage:/app/storage
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=64m
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    pids_limit: 256
    networks:
      - jrc-private

  jrc-migrate:
    image: "${JRC_MIGRATION_IMAGE:?JRC_MIGRATION_IMAGE is required}"
    build:
      context: ./server
      dockerfile: Dockerfile
      target: migration
      args:
        RELEASE_SHA: "${RELEASE_SHA:?RELEASE_SHA is required}"
    restart: "no"
    env_file:
      - path: ./server/.env
        required: false
    environment:
      DATABASE_URL: "${COMPOSE_DATABASE_URL:?COMPOSE_DATABASE_URL is required}"
    depends_on:
      jrc-db:
        condition: service_healthy
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=64m
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    pids_limit: 128
    networks:
      - jrc-private

volumes:
  jrc-database:
  jrc-private-storage:

networks:
  jrc-private:
    driver: bridge
